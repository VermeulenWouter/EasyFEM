This folder contains the Abaqus model files (``.cae``, ``.odb`` and ``.inp``) generated by the code. As an example, these files are included for the hybrid configuration with 4% NiTiCo wire volume fraction and 4% prestrain. Other files can be added by running files in the ``.source`` folder (see README there).